import { StyleSheet, Text, View } from "react-native";
import React from "react";

const bio = () => {
  return (
    <View>
      <Text>bio</Text>
    </View>
  );
};

export default bio;

const styles = StyleSheet.create({});
